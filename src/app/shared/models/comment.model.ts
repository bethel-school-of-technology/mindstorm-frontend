/**
 * The Comment class.
 * See {@link CommentService} for its use in the service.
 */
export class Comment {
  /**
   * The comment id.
   */
  id: string;
  /**
   * The comment title.
   */
  postTitle: string;
  /**
   * The comment details.
   */
  postBody: string;
  /**
   * The comment creator.
   */
  creator: string;
}
